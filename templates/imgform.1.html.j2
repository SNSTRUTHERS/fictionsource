
    <p>Uploaded images may be no larger than 4 MB.</p>
<div class="image-form" id="{{form_id}}-image-form">
    <input
        type="radio"
        class="url-radio"
        id="{{form_id}}-url-opt"
        value="url"
        name="type"
        checked
    >
    <label for="{{form_id}}-url-opt">From URL</label>

    <input
        type="radio"
        class="file-radio"
        id="{{form_id}}-file-opt"
        value="file"
        name="type"
    >
    <label for="{{form_id}}-file-opt">From File</label>

    <div class="url-subform" id="{{form_id}}-url-subform">
        <div class="form-group">
            <input type="url" id="{{form_id}}-url" name="url" placeholder="Image URL">
        </div>
    </div>

    <div class="file-subform" id="{{form_id}}-file-subform">
        <div class="form-group">
            <input
                id="{{form_id}}-file"
                name="file"
                accept="image/png, image/jpeg, image/gif, image/webp, image/tiff, image/svg+xml"
                type="file"
            >
        </div>
    </div>
</div>
